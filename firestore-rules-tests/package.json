{
  "name": "numigoo-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "firestore.rules icin regresyon testleri (Firestore emulatoru uzerinde calisir)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-numigoo \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^15.0.0"
  }
}
