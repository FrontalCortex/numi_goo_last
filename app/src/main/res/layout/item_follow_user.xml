<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingTop="14dp"
    android:paddingEnd="16dp"
    android:paddingBottom="14dp">

    <!-- Avatar circle -->
    <TextView
        android:id="@+id/tvAvatarLetter"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="22sp"
        android:textStyle="bold"
        tools:text="H"
        tools:background="#CE82FF" />

    <!-- Name + userId code -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvUserName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Hannah" />

        <TextView
            android:id="@+id/tvUserCode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#93A5B3"
            android:textSize="13sp"
            tools:text="@451289AB" />

    </LinearLayout>

    <!-- Follow back button (shown only when not following this person) -->
    <ImageButton
        android:id="@+id/btnFollowBack"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="@drawable/bg_add_friend_btn"
        android:contentDescription="Takip Et"
        android:padding="10dp"
        android:src="@drawable/ic_person_add"
        android:visibility="gone"
        app:tint="@android:color/white" />

</LinearLayout>
